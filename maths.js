function add(a, b) {
    return a + b;
}

function subtract(a, b) {
    return a - b;
}

function multiply(a, b) {
    return a * b;
}

function divide(a, b) {
    return a / b;
}


let readd = (a, b) => a + b;
let resub = (a, b) => a - b;
let remult = (a, b) => a * b;
let rediv = (a, b) => a / b;